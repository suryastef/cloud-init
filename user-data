#cloud-config
users:
  - name: suryastef
    groups: sudo
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    plain_text_passwd: cyborg
    lock_passwd: false
    shell: /bin/bash
chpasswd: { expire: False }
ssh_pwauth: True

